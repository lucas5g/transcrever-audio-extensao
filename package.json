{
  "name": "transcrevar-audio-extensao",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && cp extension/manifest.json dist/manifest.json && cp extension/styles.css dist/styles.css",
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.283",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
